<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.geometry.Insets?>

<!--
    Sidebar menu for the main application.
    Controller: main.MenuController
    Stylesheet: menu.css
    Uses VBox layout for vertical stacking of items
-->
<VBox
    fx:id="sidebarRoot"
    xmlns="http://javafx.com/javafx/8.0.171"
    xmlns:fx="http://javafx.com/fxml/1"
    fx:controller="main.MenuController"
    spacing="10"
    stylesheets="@../styles/menu.css"
    styleClass="sidebar">

    <!-- Padding around the sidebar -->
    <padding>
        <Insets top="12" right="12" bottom="12" left="12" />
    </padding>

    <!-- Top row: toggle button + brand label -->
    <HBox spacing="8" alignment="CENTER_LEFT">
        <!-- Sidebar toggle button (☰) -->
        <Button fx:id="toggleButton" text="☰" onAction="#onToggleSidebar" styleClass="toggle-button" />
        <!-- Brand/Title of sidebar -->
        <Label text="Lamesa" styleClass="brand" />
    </HBox>

    <!-- Menu buttons -->
    <Button fx:id="dashboardButton" text="Dashboard" onAction="#onDashboard"
        styleClass="menu-button">
        <graphic>
            <Label text="🏠" styleClass="icon" />
        </graphic>
    </Button>

    <Button fx:id="inventoryButton" text="Inventory" onAction="#onInventory"
        styleClass="menu-button">
        <graphic>
            <Label text="📦" styleClass="icon" />
        </graphic>
    </Button>

    <Button fx:id="ordersButton" text="Orders" onAction="#onOrders" styleClass="menu-button">
        <graphic>
            <Label text="🧾" styleClass="icon" />
        </graphic>
    </Button>

    <Button fx:id="feedbacksButton" text="Feedback" onAction="#onFeedback" styleClass="menu-button">
        <graphic>
            <Label text="💬" styleClass="icon" />
        </graphic>
    </Button>

    <Button fx:id="employeesButton" text="Employees" onAction="#onEmployees"
        styleClass="menu-button">
        <graphic>
            <Label text="👥" styleClass="icon" />
        </graphic>
    </Button>

    <Button fx:id="settingsButton" text="Settings" onAction="#onSettings" styleClass="menu-button">
        <graphic>
            <Label text="⚙️" styleClass="icon" />
        </graphic>
    </Button>

    <!-- Logout button at the bottom --> 
     <Button text="Logout" onAction="#onLogout" styleClass="menu-button logout"> 
        <graphic> 
            <Label text="↩️" styleClass="icon"/> <!-- icon for logout --> 
        </graphic> 
    </Button>

</VBox>